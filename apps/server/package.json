{
  "name": "@pcobooster/server",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "test": "vitest run --root ../.. apps/server/src",
    "typecheck": "tsc --noEmit",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "@orpc/openapi": "^1.15.0",
    "@orpc/server": "^1.15.0",
    "@orpc/zod": "^1.15.0",
    "@pcobooster/api": "workspace:*",
    "better-auth": "1.7.5",
    "hono": "^4.13.7",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@pcobooster/config": "workspace:*",
    "@types/bun": "^1.3.9",
    "tsdown": "^0.23.0",
    "typescript": "^7.0.2"
  }
}
