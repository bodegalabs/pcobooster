{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "bunVersion": "1.3.9",
  "services": {
    "web": {
      "root": "apps/web",
      "framework": "nextjs",
      "installCommand": "cd ../.. && LEFTHOOK=0 bun install --frozen-lockfile",
      "buildCommand": "cd ../.. && bun run build:web"
    },
    "server": {
      "root": "apps/server",
      "framework": "hono",
      "entrypoint": "src/index.ts",
      "outputDirectory": "dist",
      "installCommand": "cd ../.. && LEFTHOOK=0 bun install --frozen-lockfile"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": {
        "service": "server"
      }
    },
    {
      "source": "/(.*)",
      "destination": {
        "service": "web"
      }
    }
  ]
}
